# Semantic Layer Design

The semantic layer is simulated locally with YAML files. Semantic models define entities, keys, dimensions, measures, relationships, valid joins, allowed grains, ownership, and sensitive fields.
