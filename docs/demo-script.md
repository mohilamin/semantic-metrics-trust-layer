# Demo Script

1. Explain the business problem: teams calculate KPIs differently, and AI agents can retrieve unsafe definitions.
2. Show `config/metric_definitions.yaml` and certified metadata.
3. Run `python -m src.pipeline.run_all`.
4. Show certified vs uncertified metric reconciliation.
5. Show trust scorecard and AI readiness report.
6. Open the dashboard and API summary endpoints.
