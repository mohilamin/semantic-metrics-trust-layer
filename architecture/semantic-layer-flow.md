# Semantic Layer Flow

Metric definitions live in `config/metric_definitions.yaml`. The pipeline validates contracts, calculates certified metrics, compares uncertified definitions, and exposes governed results to dashboards and APIs.
