# Architecture

The platform uses synthetic enterprise data, local DuckDB storage, YAML semantic definitions, metric contracts, a calculation engine, lineage exports, trust scoring, AI readiness checks, FastAPI, and Streamlit.
